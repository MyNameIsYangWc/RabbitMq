spring:
  application:
    name: rabbitmq-producer
  cloud:
    config:
      # 配置中心的具体地址
      uri: http://localhost:6000/
      # 对应{application}部分
      name: rabbitmq-producer
      # 对应{profile}部分
      profile: dev